{
  "name": "bharat-upi-interdict",
  "version": "1.0.0",
  "private": true,
  "author": "Prashant Jagtap <jprbom@gmail.com>",
  "description": "Pre-settlement mule-network interdiction and explainable fraud investigation copilot.",
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "test": "npm --workspace backend run test && npm --workspace frontend run test",
    "audit:high": "npm audit --audit-level=high",
    "verify": "npm run build && npm run test && npm run audit:high"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "vite": "^6.4.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jprbom/bharat-upi-interdict.git"
  },
  "homepage": "https://github.com/jprbom/bharat-upi-interdict#readme",
  "bugs": {
    "url": "https://github.com/jprbom/bharat-upi-interdict/issues"
  }
}
